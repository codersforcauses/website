import { css } from '@emotion/core'

export const styles = theme => css`
  .event-img {
    max-height: 15rem;
  }
  .heading {
    font-size: 1.5rem;
    font-weight: bold;
    line-height: 1.7rem;
  }
  .smaller {
    line-height: 1.3;
  }
`
