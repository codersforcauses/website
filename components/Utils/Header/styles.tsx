import { css } from '@emotion/react'

export const styles = theme => css`
  min-height: 64px;

  .brand {
    line-height: 1.6;
  }

  .icon {
    font-size: 1.4rem;
    vertical-align: middle;
    font-weight: bolder;
  }
`
