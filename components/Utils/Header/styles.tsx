import { css } from '@emotion/core'

export const styles = theme => css`
  min-height: 5vh;

  .brand {
    line-height: 1.8rem;
  }

  .icon {
    font-size: 1.4rem;
    vertical-align: middle;
    font-weight: bolder;
  }
`
