import React from 'react'

export default (
  <style jsx>{`
    .client-logo {
      object-fit: contain;
      object-position: center;
      max-height: 100px;
      filter: grayscale(100%) contrast(0.2) brightness(1.1);
    }
  `}
  </style>
)
