import { css } from '@emotion/core'

export const styles = theme => css`
  input:read-only {
    background-color: white;
  }
  .menu {
    width: 100%;
    max-height: 12rem;
    overflow-y: auto;
  }
  .icon {
    font-size: 20px;
  }
`
