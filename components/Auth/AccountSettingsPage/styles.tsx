import { css } from '@emotion/core'
import { media } from 'lib/mediaQueries'

export const styles = theme => css`
  .avatar {
    margin-top: -73px;
  }
  .text-area {
    min-height: 10rem;
  }

  ${media.sm`
  `}
`
